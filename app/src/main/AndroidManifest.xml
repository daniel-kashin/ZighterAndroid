<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zighter.zighterandroid">

    <uses-permission android:title="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:title="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:title="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:title="android.permission.INTERNET" />

    <application
        android:title=".ZighterApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:title=".presentation.bottom_navigation.BottomNavigationActivity">
            <intent-filter>
                <action android:title="android.intent.event.MAIN" />
                <category android:title="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:title="ACTION_OPEN_ACTIVITY" />
            </intent-filter>
        </activity>

        <activity android:title=".presentation.excursion.holder.ExcursionHolderActivity" />

        <activity android:title=".presentation.media.MediaActivity" />

        <receiver
            android:title=".data.job_manager.download_excursion.DownloadExcursionNotificationBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <action android:title="ACTION_CANCEL_JOB" />
            </intent-filter>
        </receiver>

    </application>

</manifest>